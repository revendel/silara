---
/**
 * Header component.
 * WordPress equivalent: header.php / get_header().
 *
 * Desktop (≥ 1024 px): Logo left + inline nav links right.
 * Mobile  (< 1024 px): Logo left + "Menu" pill button right
 *                       → triggers MenuOverlay.
 */
import Logo from './Logo.astro';
import Line from './Line.astro';

const navLinks = [
  { href: '/',            label: 'Vision'      },
  { href: '/mission',     label: 'Mission'     },
  { href: '/our-product', label: 'Our Product' },
  { href: '/the-future',  label: 'The Future'  },
  { href: '/contact',     label: 'Contact'     },
];

const currentPath = Astro.url.pathname;
---

<header class="site-header" transition:name="site-header" transition:animate="none">
  <div class="site-header__inner">
    <Logo variant="smaller" />

    <!-- Desktop nav -->
    <nav class="site-header__nav" aria-label="Primary navigation">
      <ul class="site-header__nav-list" role="list">
        {navLinks.map(link => (
          <li>
            <a
              href={link.href}
              class:list={[
                'site-header__nav-link',
                { 'is-active': currentPath === link.href || (link.href !== '/' && currentPath.startsWith(link.href)) }
              ]}
            >
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <!-- Pill button — desktop only -->
    <button
      class="site-header__menu-btn"
      aria-label="Open navigation menu"
      aria-expanded="false"
      aria-controls="menu-overlay"
      id="menu-toggle"
    >
      Menu
    </button>
  </div>
  <Line />
</header>

<style>
  .site-header {
    display: flex;
    flex-direction: column;
    background-color: var(--base);
    flex-shrink: 0;
    position: relative;
    z-index: 80;
  }

  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 var(--scale-8);
    height: var(--scale-9);
  }

  /* ── Desktop nav ── */
  .site-header__nav-list {
    display: flex;
    align-items: center;
    gap: var(--scale-6);
    list-style: none;
  }

  .site-header__nav-link {
    font-size: var(--scale-6); /* 21px */
    line-height: var(--scale-7);
    padding: var(--scale-4);
    white-space: nowrap;
    opacity: 0.5;
    transition: opacity 0.15s ease;
  }

  .site-header__nav-link:hover {
    opacity: 1;
  }

  .site-header__nav-link.is-active {
    opacity: 1;
  }

  /* ── Pill button (desktop only) ── */
  .site-header__menu-btn {
    display: none;
    font-size: var(--scale-6);
    line-height: var(--scale-6);
    color: var(--base);
    background-color: var(--primary);
    padding: var(--scale-5) var(--scale-6);
    border-radius: var(--scale-9);
    white-space: nowrap;
    transition: opacity 0.15s ease;
  }

  .site-header__menu-btn:hover {
    opacity: 0.75;
  }

  /* ── Responsive ── */
  @media (max-width: 1023px) {
    .site-header__inner {
      padding: var(--scale-7);
    }

    .site-header__nav {
      display: none;
    }

    /* Top bar mobile: logo only, no button */
    .site-header__menu-btn {
      display: none;
    }
  }
</style>
